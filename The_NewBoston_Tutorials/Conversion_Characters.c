#include<stdio.h>
#include<stdlib.h>

int main()
{

	printf("%s is the the best  %s ever\n", "Pavel", "programmer");
	// %s is placeholder for strings

	printf("I ate %d pizzas last night", 4);
	// %d is placeholder for numbers ,whenever we want to use a float %f is used.
	//if we want to use e.g. only two places after the decimal point '%.2f' is used.

	return 0;



}